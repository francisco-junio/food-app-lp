import { RestaurantList } from "./RestaurantList"

export const RestaurantSection = () => {
    return(
        <section>
            <h2>Restaurantes famosos</h2>
            <RestaurantList />
        </section>
    )
}